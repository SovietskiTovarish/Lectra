import 'package:lectra/bootstrap.dart';

/// Application entry point.
///
/// All initialization work is delegated to [bootstrap] so that
/// [main] stays a single, predictable line.
void main() {
  bootstrap();
}